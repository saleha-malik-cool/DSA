/*
Question:-
making pattern of nXn square :
for example 
n=4
****
****
****
****
*/
 
//Code:

#include <iostream>
using namespace std;

int main() {

    int n;
    cin >> n;

    int i = 1;

    while(i <= n) {

        int j = 1;

        while(j <= n) {
            cout << "*";
            j = j + 1;
        }

        cout << endl;
        i = i + 1;
    }

    return 0;
}
/*
Approach:-

1.Take input n from the user for the size of square pattern.
2.Initialize row variable i = 1.
3.Run outer while loop until i <= n to print rows.
4.Inside outer loop initialize column variable j = 1.
5.Run inner while loop until j <= n to print stars in each row.
6.Print "*" inside inner loop.
7.Increment j after printing each star.
8.After inner loop ends move to next line using cout << endl.
9.Increment i to move to next row.
10.Repeat the process until all rows are printed.
*/
//Time Complexity:-O(N²)
//Space Complexity:-O(1)
